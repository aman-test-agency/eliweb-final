# EliWeb Backend (API only)

REST API for EliWeb.in. See the [root README](../README.md) for setup and deployment.

```bash
npm install
cp .env.example .env
npm run db:push
npm run dev
```

Runs on port **5000** via `node server.js`.
